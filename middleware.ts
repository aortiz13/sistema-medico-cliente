import { createServerClient, type CookieOptions } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function middleware(request: NextRequest) {
  // Crea una respuesta inicial que pasa la petición adelante
  let response = NextResponse.next({
    request: {
      headers: request.headers,
    },
  })

  // Crea un cliente de Supabase para el lado del servidor
  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        get(name: string) {
          return request.cookies.get(name)?.value
        },
        set(name: string, value: string, options: CookieOptions) {
          // Si necesitamos establecer una cookie, actualizamos tanto la petición como la respuesta
          request.cookies.set({
            name,
            value,
            ...options,
          })
          response = NextResponse.next({
            request: {
              headers: request.headers,
            },
          })
          response.cookies.set({
            name,
            value,
            ...options,
          })
        },
        remove(name: string, options: CookieOptions) {
          // Si necesitamos eliminar una cookie, hacemos lo mismo
          request.cookies.set({
            name,
            value: '',
            ...options,
          })
          response = NextResponse.next({
            request: {
              headers: request.headers,
            },
          })
          response.cookies.set({
            name,
            value: '',
            ...options,
          })
        },
      },
    }
  )

  // Esta línea es crucial: refresca la sesión del usuario en el servidor.
  await supabase.auth.getUser()

  // Devuelve la respuesta, que ahora contiene las cookies de sesión actualizadas.
  return response
}

// Configuración para que el middleware se ejecute en todas las rutas excepto en los archivos estáticos.
export const config = {
  matcher: [
    /*
     * Match all request paths except for the ones starting with:
     * - _next/static (static files)
     * - _next/image (image optimization files)
     * - favicon.ico (favicon file)
     */
    '/((?!_next/static|_next/image|favicon.ico).*)',
  ],
}
